class Solution:
    def reverseBits(self, n: int) -> int:
       
        result = 0

        for _ in range(32):
            bit = n % 2
            result = result * 2 + bit
            n //= 2

        return result
